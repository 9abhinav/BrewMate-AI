# BrewMate AI Environment Configuration Template

# Google Gemini API Key (Required for live Gemini LLM calls; agent falls back gracefully if absent)
GEMINI_API_KEY=your_gemini_api_key_here

# Server Port (Default: 8000)
PORT=8000

# Environment Mode (development / production)
ENVIRONMENT=production
